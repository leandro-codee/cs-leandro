// src/helpers/loading.jsx
import React from 'react';

// Componente Loading con un spinner o mensaje de carga
const Loading = () => (
  <div className="loading-spinner">
    <p>Loading...</p>
  </div>
);

export default Loading;